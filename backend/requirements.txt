# Web Framework
fastapi==0.141.1
# Pinado direto, e nao so pelo fastapi: a correcao do DoS quadratico por
# cabecalho Range no FileResponse (PYSEC-2026-1942) esta na 0.49.1, e o
# /files/{token} e publico e usa FileResponse.
starlette==0.51.0
uvicorn[standard]==0.34.0

# Database
sqlalchemy[asyncio]==2.0.40
alembic==1.15.2
asyncpg==0.30.0

# Validation & Settings
pydantic==2.11.3
pydantic-settings==2.9.1
email-validator==2.2.0

# Authentication
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.31
# Segundo fator (TOTP). Wheel pura, sem dependencia transitiva.
pyotp==2.10.0
# Ja vinha como transitiva do python-jose, mas agora e importada direto
# (Fernet, em services/mfa.py) — dependencia direta se declara e se pina.
cryptography==50.0.0

# Rate limiting
slowapi==0.1.9

# Storage (MinIO)

# Redis (cache, blacklist de token e lock da rotina de fechamento)
redis==5.2.1

# Email
fastapi-mail==1.4.2

# HTTP Client (for LLM integrations)
httpx==0.28.1

# Logging
loguru==0.7.3

# Utils
python-dotenv==1.1.0
python-dateutil==2.9.0
pytz==2025.2

# PDF generation
reportlab==4.4.0
