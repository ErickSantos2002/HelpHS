# Opcional em dev: sem esta variável o app usa /api/v1 e o proxy do Vite
# (vite.config.ts) encaminha para o backend local em localhost:8001.
# Obrigatória como build arg no Docker/EasyPanel (URL pública da API).
VITE_API_URL=http://localhost:8001/api/v1
